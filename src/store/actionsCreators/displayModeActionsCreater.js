import ACTION_SET_COLOR_MODE from '../actions/displayModeActions';

const displayActionsSetColorMode = payload => ({ type: ACTION_SET_COLOR_MODE, payload });

export default displayActionsSetColorMode;
